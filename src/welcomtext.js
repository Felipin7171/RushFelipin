const welcome = (number, groupname) => {
    return `Salve irmão (ã) @${number}. Seja bem-vindo ao grupo ${groupname}, espero que se divirta 🤬🐊`
}
exports.welcome = welcome

const bye = (number) => {
    return `@${number} Deitou Complex 🛌👋`
}
exports.bye = bye